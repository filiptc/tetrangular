define(function () {
  'use strict';
  return function (exampleCollection) {
    exampleCollection.exampleMethod = function () {
      // do stuff with collection
    };

    return exampleCollection;
  };
});
